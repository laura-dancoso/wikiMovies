import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class SettingsService {

  //acá vamos a setear en el local storage (o similar) los datos como el nombre del usuario, las preferencias de generos y peliculas
  constructor() { }
}
